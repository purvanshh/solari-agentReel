# AgentReel demo

Minimal Solari agent that uses `recorded_session()`.

```bash
export SOLARI_API_KEY=slr_live_...
pip install -e ../..   # from repo root: pip install -e .
agentreel run main.py --no-git
```
